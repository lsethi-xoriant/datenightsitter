<!-- Fixed navbar -->
<div class="navbar navbar-default navbar-static-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">
        <img src="/images/logo-bw.png" width="150" height="37.125" ALT="Sittercity">
      </a>
    </div>
    <div class="navbar-collapse collapse" style="height: auto;">
      <ul class="nav navbar-nav">
        <% if member_signed_in? %>
          <li>
            <%= link_to "Dashboard", dashboard_member_path(current_member) %>
          </li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Tools<b class="caret"></b></a>
            <ul class="dropdown-menu">
              <% if current_member.is_a?(Provider) %>
                <li><%= link_to "Settle Up", settle_up_member_path(current_member) %></li>
              <% end %>
            </ul>
          </li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Settings<b class="caret"></b></a>
            <ul class="dropdown-menu">
              <li><%= link_to "Account Profile", profile_member_path(current_member) %></li>
              <% if current_member.is_a?(Provider) %>
                <li><%= link_to "Bank Account", bank_account_member_path(current_member) %></li>
              <% end %>
            </ul>
          </li>
        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if member_signed_in? %>
          <li class="active"><%= link_to 'Log out', log_out_path %></li>
        <% else %>
          <!--
          <li><%= link_to "Sign up", sign_up_path %></li>
          -->
          <li class="active"><%= link_to "Log in", log_in_path %></li>
        <% end %>
      </ul>
    </div><!--/.nav-collapse -->
  </div>
</div>